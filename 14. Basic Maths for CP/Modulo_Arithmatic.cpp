#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <numeric>
#include <string>
#include <map>
#include <set>
#include <limits.h>
using namespace std;

// Modulo operator gives remainder
// Range of int is -2^31 to 2^31-1
// Properties of mod operator-
// 1)(a + b) % m = a % m + b % m
// 2)a % m - b % m = (a - b) % m
// 3)((a % m) % m) % m = a % m
// 4)a % m * b % m = (a * b) % m

int main()
{

    return 0;
}